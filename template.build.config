# Build Configuration Template
# Copy this file and customize it for your project

# === PROJECT CONFIGURATION ===
project_name=MyApp
project_dir=/path/to/your/project
output_dir=/path/to/your/project/updates
skip_confirmation=false

# Define the build steps (comma-separated)
# Available steps: Cleanup updates directory,Build DMG,Notarize DMG,Staple & Validate DMG,Generate Sparkle Appcast
steps=Cleanup updates directory,Build DMG,Notarize DMG,Staple & Validate DMG,Generate Sparkle Appcast

# === PROJECT-SPECIFIC VARIABLES ===
updates_dir=/path/to/your/project/updates
dmg_name=MyApp.dmg
background_image=/path/to/your/project/background.jpg
developer_id=Developer ID Application: Your Name (TEAMID)
team_id=TEAMID

# === KEYCHAIN REFERENCES ===
# Store your Apple ID and app-specific password in keychain with these service names
apple_id_keychain_service=MyAppDevEmail
app_password_keychain_service=MyAppAppPassword

# === SPARKLE CONFIGURATION ===
# The script will auto-detect Sparkle directory, but you can override it here
# sparkle_dir=/path/to/sparkle/directory

# === DMG CONFIGURATION ===
dmg_window_size=600,400
dmg_icon_size=128
dmg_text_size=16